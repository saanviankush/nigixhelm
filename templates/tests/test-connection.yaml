apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "boad.fullname" . }}-test-connection"
  labels:
    {{- include "boad.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "boad.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
