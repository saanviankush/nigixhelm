apiVersion: v1
kind: Service
metadata:
  name: {{ include "boad.fullname" . }}
  labels:
    {{- include "boad.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "boad.selectorLabels" . | nindent 4 }}
